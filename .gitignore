# Secrets / local config
.env
.env.local

# Runtime state written by the bot
jenkinsupdate.json
# Jenkins job/env/service catalogues learned at runtime - never commit these,
# a tracked copy makes `git pull` abort on the server and breaks /deploy.
jenkins_job_env_services.json
cpms_igo_uat_services.json
bi_script_deployment_files.json
# Debug output from `maintenance_mail.py jenkins-reply-preview`
reply_preview.html
# Reply-email engine (maintenance_mail.py) runtime state
maintenance.json
allemail.json
egs.json
egstest.json
.prod_batch_sm_sessions.json
*.log

# Playwright persistent profiles / browser data
.fpms-playwright-profile/
*-playwright-profile/
browser_data/
browser_state.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
__MACOSX/
.idea/
.vscode/
tests/__pycache__/
